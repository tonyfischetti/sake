language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
  - pip install .
  - pip install -r ./testlib/test_reqs.txt
# command to run tests
script: python ./test_sake.py
