
get url

put source url in lib/constants

look at /Library/Python/2.7/site-packages and populate
fields missing from the egg info
forgot yaml as dependency
if no formula then its meta
a help message is mandatory
check if metatarget has atomtargets
allow for alternate sakefilenames
meta-targets should have nothing but help and atom targets
check if all has unrecognized target
issue warning if target all is missing
yaml parse accepts duplicate values
¿can meta-targets be nested?
duplicates are a sign of a really really big bug
¿tests?
docs man file
